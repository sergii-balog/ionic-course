export interface BookingDTO {
  placeId: string;
  userId: string;
  placeTitle: string;
  firstName: string;
  lastName: string;
  guestNumber: number;
  dateFrom: string;
  dateTo: string;
  image: string;
}
